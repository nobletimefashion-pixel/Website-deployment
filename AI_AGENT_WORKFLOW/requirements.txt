# ── RENDER DEPLOYMENT — slim build ───────────────────────────────────────────
# Heavy optional tools (playwright, moviepy, RAG, etc.) are NOT installed here.
# They will fail gracefully with "install X to use this tool" if called.
# This keeps the build under Render's 512 MB free-tier RAM limit.

# Web UI server
fastapi
uvicorn[standard]
websockets

# LLM client
openai

# MCP
fastmcp

# Config / CLI
click
tomli
platformdirs
pydantic

# Terminal UI (still used for logging)
rich

# Token counting
tiktoken

# HTTP clients
httpx
requests

# Web scraping
beautifulsoup4
ddgs

# PDF / DOCX generation
reportlab
python-docx

# Anthropic SDK (software_architect tool)
anthropic

# OS monitoring
psutil

# OSINT - lightweight ones only
python-whois